{{!< default}}

<style>

    .kg-image-card img {
margin-left: 0;
width: 150px;
margin-bottom: -50px;
margin-top: 50px;
    }

#icon {
    margin-bottom: 10px !important;
}
    h1 {
        margin-top: 20px;
        color: var(--color-hover);
    }
    .comments-area.inner {
        max-width: 700px;
        }
    h3 {
        font-size: 20px;
        letter-spacing: normal;
        text-transform: none;
        font-weight: 700 !important;
        margin-top: 50px;
    }
    h3:first-of-type {
        margin-top: 0;
    }
    h4 {
        color: #fff;
        padding-top: 30px;
    }
    blockquote {
        padding-left: 0;
        margin-top: 50px;
        border-left: none;
    }
    blockquote h1 {
        font-size: 60px;
        line-height: .9;
        color: #222
    }
    blockquote cite {
        color: #333;
    }
    p {
        margin-top: 5px;
        margin-bottom: 25px;
        font-weight: 400;
        font-family: var(--font-sans);
    }
    ul, ol {
        margin-top: 30px;
        margin-left: 10px;
        margin-right: 50px;
        margin-bottom: 30px;
    }
    li {
        margin-bottom: 15px;
        line-height: 1.5;
        font-size: var(--type-body);
        font-family: var(--font-sans) !important;
    }
    ul li::marker {
    color: #444;
}
    .post-content ol li, .post-content ul li {
        font-family: var(--font-sans);
        padding-bottom: 8px;
    }

    /* Spite Hero (only shows on main Spite page) */
    .spite-hero {
        width: 100%;
        max-width: 1300px;
        margin: 0 auto 60px;
        background: #f0f0f0;
        border-bottom-right-radius: 5px;
        border-bottom-left-radius: 5px;
        overflow: hidden;
        padding: 5%;
        padding-bottom: 30px;
        box-sizing: border-box;
        }

    .spite-header {
        margin-top: 20px;
        margin-bottom: 30px;
        }

    .spite-copy {
        padding-top: 20px;
    }

    .spite-screenshot {
        position: relative;
        width: 100%;
        aspect-ratio: 300 / 135;
        overflow: hidden;
        }

    .spite-buttons {
        margin-left: 5%
        }


.spite-screenshot img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
    position: absolute;
    background: #f0f0f0;
    top: 0;
    left: 0;
    opacity: 0;
    transition: opacity 1s ease-in-out;
    }

.spite-screenshot img:first-child {
    position: relative;
    opacity: 1;
    }

.spite-screenshot img.active {
    opacity: 1;
    }

/* Left-aligned title and tagline */
.post-header-content {
    text-align: left;
    }

.post-header-content h1 {
    text-align: left;
    margin-bottom: 20px;
    }

.post-header-content + .post-content > p:first-child {
    text-align: left;
    font-size: 1.1em;
    margin-bottom: 40px;
    }

    /* Navigation */
    .app-nav {
        display: flex;
        gap: 40px;
        margin-top: 30px;
        justify-content: left;
        border-radius: 30px;
        width: 55%;
        min-width: 300px;
        background: #f0f0f0;
        padding-left: 30px;
        align-items: center;
    }

    li::marker {
    font-size: 13px; /* or whatever size you want */
    }

    .app-nav a {
        font-size: var(--type-body-sm);
        font-weight: 700;
        color: var(--color-hover);
    }

    .mobile-download {
        display: none;
    }

    .app-nav button {
        font-weight: 700;
        background-color: #000;
        border-bottom: none;
        font-size: 14px;
        margin-left: auto;
        color: #fff;
    }

    [data-theme="dark"] .app-nav button {
        background-color: #fff;
        color: #000;
    }   

   .spite-sidebar button {
        font-weight: 700;
        background-color: #000;
        border-bottom: none;
        font-size: 14px;
        margin-left: 30px;
        color: #fff;
    }
    .spite-sidebar button:hover {
        background: #fff !important;
    }
    .app-nav a:hover,
    .app-nav a.active {
        opacity: 1;
        color: var(--color-hover);
        text-decoration: underline;
        text-underline-offset: 10px;
        text-decoration-thickness: 3px;
    }



    @media (max-width: 700px) {
        .spite-screenshot {
        height: auto;
        padding-top: 70px;
        }

            .app-nav button {
                display: none;
            }

       .mobile-download {
        display: block;
        margin-top: 20px;
        font-size: 14px;
        padding: 10px 26px;
    }


        .spite-screenshot img {
            position: relative !important;
        }

        .spite-hero {
            padding: 0;
            width: 88%;
            margin-left: auto;
            margin-right: auto;
            margin-bottom: 30px;
        }
        ul, ol {
            margin-left: 0;
            margin-right: 0;
            margin-top: 0;
            margin-bottom: 20px;
        }
        p, ul li, ol li { font-size: 14px;}

        .app-nav a {
            font-size: 16px;
        }   

        p { margin-bottom: 10px;}

        blockquote h1 {
            font-size: 30px;
        }
        blockquote {
            padding-left: 0px;
            border-left: none;
        }

        .app-nav {
            margin-bottom: 0;
        }
    }

    .radio-page .radio-content-wrapper {
        padding-left: 5%;
        padding-right: 5%;
        }

    .spite-columns {
        display: grid;
        grid-template-columns: 60% 30%;
        gap: 10%;
        align-items: start;
        }

    .spite-sidebar {
        /* right column */
        position: sticky;
        top: 40px;
        }

    .footer-tags {
        display: none;
        }

    h6 {
        font-size: 12px;
font-weight: 500;
line-height: 1.7;
padding-left: 20px;
border-left: 3px solid var(--color-text);
        }

    h6 a { 
        text-decoration: underline;
        text-underline-offset: 3px;
        text-decoration-thickness: 0.5px;
                font-size: 12px;
font-weight: 500;
        }

li a { 
        text-decoration: underline;
        text-underline-offset: 3px;
        text-decoration-thickness: 0.5px;
        }

    h3 + ol, h3 + ul, h4 + ul {
        margin-top: 25px !important;
    }

    @media (max-width: 900px) {
        .spite-columns {
            grid-template-columns: 1fr;
            gap: 0;
        }
    }

    h5.kg-toggle-heading-text {
        font-size: 16px !important;
        text-transform: none !important;
        letter-spacing: normal !important;
        font-weight: 600;
    }
    .kg-toggle-card-icon svg {
        color: #fff;}

    [data-theme="dark"] .kg-toggle-card-icon svg {
        color: #000;
    }



[data-theme="dark"] .spite-screenshot img, [data-theme="dark"] .spite-hero, , [data-theme="dark"] .app-nav {
    background: #333;
    }
</style>

<main id="main" class="content outer radio-page">

    {{#post}}
    
    <!-- Hero (only on main Spite page) -->
    <div class="spite-hero">
        <div class="spite-screenshot">
            <img src="{{asset "spite/spite1.webp"}}" alt="Spite" class="active">
            <img src="{{asset "spite/spite2.webp"}}" alt="Spite">
            <img src="{{asset "spite/spite3.webp"}}" alt="Spite">
            <img src="{{asset "spite/spite4.webp"}}" alt="Spite">
            <img src="{{asset "spite/spite5.webp"}}" alt="Spite">
        </div>
    </div>
        

    <div class="radio-content-wrapper">
        <div class="spite-header">
            <h1>Spite</h1>
            <p>A dignified music player like it's 2006 but a little nicer.</p>
            <button class="mobile-download"><a href="https://apps.apple.com/us/app/spite-music-player/id6757397964"></a>Download on the App Store</a></button>
        </div>
        <!-- Auto-detecting navigation -->
        <nav class="app-nav">
            <a href="/spite/" data-slug="spite">Features</a>
            <a href="/spite-test-center/" data-slug="spite-test-center">Learning Center</a>
            <a href="/spite-privacy/" data-slug="spite-privacy">Privacy</a>
            <button><a href="https://apps.apple.com/us/app/spite-music-player/id6757397964"></a>Download on the App Store</a></button>

        </nav>
        <script>
        // Highlight active nav item
        (function() {
            const currentSlug = '{{slug}}';
            const links = document.querySelectorAll('.app-nav a[data-slug]');
            links.forEach(link => {
                if (link.getAttribute('data-slug') === currentSlug) {
                    link.classList.add('active');
                }
            });
        })();
        </script>

        <div class="inner-wide">
    <article class="spite-columns">
        <div class="spite-copy">
            {{content}}
            
            {{#has slug="spite-test-center"}}
                {{> comments}}
            {{/has}}
        </div>

        <div class="spite-sidebar">
            {{#has slug="spite"}}
                <!-- Simon Critchley quote -->
                <blockquote><a href="#">
                    <h1>It is impossible to be an atheist when listening to the music that one loves.</h1>
                    <cite>Simon Critchley</cite></a>
                </blockquote>
            {{else}}
                <!-- Download buttons -->
                <div class="spite-buttons">
                    <h6>Heavy duty thanks to <a href="http://candychang.com">Candy Chang</a>, <a href="https://timbornholdt.com">Tim Bornholdt</a>, <a href="https://www.thetonearm.com">Michael Donaldson</a>, <a href="http://oliverfarshi.com">Oliver Farshi</a>, <a href="https://hicks.design">Jon Hicks</a>, <a href="http://davidmead.com">David Mead</a>, <a href="https://danparham.com">Dan Parham</a>, and <a href="http://ruk.ca">Peter Rukavina</a></h6>
                </div>
            {{/has}}
        </div>
    </article>
</div>
    </div>
    <br>




<!-- EPISODES -->
    <div class="inner-wide">
        <div class="grid grid-more">
            {{#get "posts" include="authors,tags,tiers" filter="tag:midnight-radio+id:-{{id}}" limit="3"}}
            {{#foreach posts visibility="all"}}
                <article>
                    <a href="{{url}}" class="post-card-link">
                    <div class="post-card bg-dark bg-full">

                        {{#if feature_image}}
                            <img
                                class="post-card-image bg-full-image bg-fade-in"
                                srcset="{{img_url feature_image size="xs"}} 300w, {{img_url feature_image size="s" }} 600w, {{img_url feature_image size="m" }} 800w, {{img_url feature_image size="l" }} 1200w, {{img_url feature_image size="xl" }} 1600w, {{img_url feature_image size="xxl" }} 2000w"
                                sizes="(min-width: 1600px) 1600px, 100vw"
                                src="{{img_url feature_image size="xl"}}"
                                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                            />
                        {{/if}}

                        <div class="post-card-inside">
                            <div class="post-card-content">          
                                <div class="big-title">
                                    <h5>
                                        Midnight Radio 
                                        {{#foreach tags visibility="internal"}}
                                        {{#if (match slug "^hash-mr-")}}
                                            <span class="episode-number" data-ep="{{slug}}"></span>
                                        {{/if}}
                                        {{/foreach}}
                                    </h5>
                                    <h2>{{title}}</h2>
                                </div>
                            </div>
                        </div>
                    </div>
                    </a>
                </article>                            

            {{/foreach}}
            {{/get}}
        </div>
    </div>
    {{/post}}


</main>




<script>
(function() {
    const container = document.querySelector('.spite-screenshot');
    const images = Array.from(container.querySelectorAll('img'));

    // Shuffle images randomly
    for (let i = images.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [images[i], images[j]] = [images[j], images[i]];
    }

    // Re-append in shuffled order
    images.forEach(img => container.appendChild(img));

    // Reset active state to first (now random) image
    container.querySelectorAll('img').forEach((img, i) => {
        img.classList.toggle('active', i === 0);
    });

    let imageIndex = 0;
    const shuffledImages = container.querySelectorAll('img');

    function cycleImages() {
        if (shuffledImages.length <= 1) return;
        shuffledImages[imageIndex].classList.remove('active');
        imageIndex = (imageIndex + 1) % shuffledImages.length;
        shuffledImages[imageIndex].classList.add('active');
    }

    setInterval(cycleImages, 7000); // Cycle every 7 seconds
})();
</script>