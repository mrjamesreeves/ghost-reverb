{{!< default}}

{{!-- /radio/ — latest episode as a full post; the cover grid below
     is the subnav. Literal tag filter — {{#get}} can't interpolate. --}}

{{#contentFor "title"}}Midnight Radio — {{@site.title}}{{/contentFor}}

{{#get "posts" filter="tag:midnight-radio" limit="all" order="published_at desc" include="tags"}}
  {{#foreach posts limit="1"}}
    {{> "post-view"}}
  {{/foreach}}
  {{> "mr-grid"}}
{{/get}}
