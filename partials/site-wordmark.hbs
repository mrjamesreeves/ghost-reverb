{{!-- Global wordmark / home link.
     Included inside each page's header/watermark container so it
     stacks above the section title and can't collide with anything.
     Not rendered on the homepage — that template doesn't include this
     partial. Styles in style.css: search ".site-wordmark". --}}
<a class="site-wordmark" href="{{@site.url}}/" aria-label="James A. Reeves — home">James A. Reeves</a>
