{{!-- Full post view. Expects a post context — used by post.hbs and,
     for the latest post, by every channel template (one item per
     page; the channel's subnav renders below, by the CALLER).

     Two variants:
       - Midnight Radio: black 1:1 hero (eyebrow + huge title + cover
         art), right marginalia rail (Download / Podcast / Tracklist —
         populated by zen.js — plus the signup CTA), body column.
       - Everything else: feature image (900px, 577 crop), Bebas
         title, body column. --}}

{{#has tag="Midnight Radio"}}

  <article class="post--mr {{post_class}}" data-slug="{{slug}}">

    <div class="mr-hero">
      <div class="mr-hero-text">
        <p class="mr-hero-eyebrow">
          Midnight Radio
          {{#foreach tags visibility="internal"}}<span class="episode-number" data-ep="{{slug}}"></span>{{/foreach}}
        </p>
        <h1 class="mr-hero-title">{{title}}</h1>
      </div>
      {{#if feature_image}}
        <img class="mr-hero-art"
          srcset="{{img_url feature_image size="m"}} 800w, {{img_url feature_image size="l"}} 1200w, {{img_url feature_image size="xl"}} 1600w"
          sizes="(max-width: 960px) 100vw, 900px"
          src="{{img_url feature_image size="l"}}"
          alt="{{title}}" loading="eager">
      {{/if}}
    </div>
    {{#if feature_image_caption}}
      <div class="post-image-caption mr-hero-caption">{{{feature_image_caption}}}</div>
    {{/if}}

    {{!-- Marginalia rail: zen.js clones the Download/Podcast anchors
         out of the post's first h4, adds a Tracklist anchor when the
         post has an <ol>, and fades the rail on scroll. Non-members
         get the signup CTA beneath the links. --}}
    <aside class="marginalia" id="marginalia" aria-label="Episode links">
      <div class="marginalia-inner">
        <div id="marginaliaLinks"></div>
        {{#unless @member}}
          <div class="marginalia-cta">
            <p class="cta-title">Join Midnight Radio</p>
            <p class="cta-blurb">Notes on surviving the 21st century and a mixtape delivered &rsquo;round midnight on the 1st and 15th of the month.</p>
            <button class="cta-button" data-signup>Subscribe</button>
          </div>
        {{/unless}}
      </div>
    </aside>

    <div class="post-body-col">
      <div class="post-content">
        {{content}}
      </div>
      <div class="post-meta">
        <span>{{date format="MMMM D, YYYY"}}</span>
        <span class="post-meta-sep">&bull;</span>
        {{#if @site.comments_enabled}}
          <button class="post-meta-comments" data-comments-toggle aria-expanded="false">{{comment_count empty="Comments" singular="comment" plural="comments"}}</button>
          <span class="post-meta-sep">&bull;</span>
        {{/if}}
        <a class="post-meta-share" href="#/share">Share</a>
        <span class="post-arrows" id="postArrows" hidden>
          <a class="post-arrow-prev" id="arrowPrev" aria-label="Previous"><img src="{{asset "img/arrow-rev.svg"}}" alt=""></a>
          <a class="post-arrow-next" id="arrowNext" aria-label="Next"><img src="{{asset "img/arrow-fwd.svg"}}" alt=""></a>
        </span>
      </div>
      {{#if @site.comments_enabled}}
        <div class="comments-body" data-comments-body hidden>
          {{comments title="" count=false}}
        </div>
      {{/if}}
    </div>

  </article>

{{else}}

  <article class="{{post_class}}" data-slug="{{slug}}">

    {{#if feature_image}}
      <figure class="post-image">
        <img
          srcset="{{img_url feature_image size="m"}} 800w, {{img_url feature_image size="l"}} 1200w, {{img_url feature_image size="xl"}} 1600w"
          sizes="(max-width: 960px) 100vw, 900px"
          src="{{img_url feature_image size="l"}}"
          alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
          loading="eager">
        {{#if feature_image_caption}}
          <figcaption class="post-image-caption">{{{feature_image_caption}}}</figcaption>
        {{/if}}
      </figure>
    {{/if}}

    <div class="post-body-col">
      <h1 class="post-title">{{title}}</h1>
      <div class="post-content">
        {{content}}
      </div>
      <div class="post-meta">
        <span>{{date format="MMMM D, YYYY"}}</span>
        <span class="post-meta-sep">&bull;</span>
        {{#if @site.comments_enabled}}
          <button class="post-meta-comments" data-comments-toggle aria-expanded="false">{{comment_count empty="Comments" singular="comment" plural="comments"}}</button>
          <span class="post-meta-sep">&bull;</span>
        {{/if}}
        <a class="post-meta-share" href="#/share">Share</a>
        <span class="post-arrows" id="postArrows" hidden>
          <a class="post-arrow-prev" id="arrowPrev" aria-label="Previous"><img src="{{asset "img/arrow-rev.svg"}}" alt=""></a>
          <a class="post-arrow-next" id="arrowNext" aria-label="Next"><img src="{{asset "img/arrow-fwd.svg"}}" alt=""></a>
        </span>
      </div>
      {{#if @site.comments_enabled}}
        <div class="comments-body" data-comments-body hidden>
          {{comments title="" count=false}}
        </div>
      {{/if}}
    </div>

  </article>

{{/has}}
