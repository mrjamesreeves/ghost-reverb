{{!< default}}

{{!-- /favorites/ — section landing. Renders the same reader as individual
     favorite post URLs (post.hbs dispatches there too when primary_tag is
     "favorites"). Both routes share partials/favorites/_reader.hbs. Body
     type inherits the site default (IBM Plex Sans via Adobe Typekit). --}}

{{> "favorites/_reader"}}
